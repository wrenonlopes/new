<section class="section" style="background:var(--cream)">
  <div class="wrap">
    <span class="kicker">{{ page.title }}</span>
    <h1 class="h-xl">{{ page.title }}</h1>
    <div class="rte" style="margin-top:30px">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
